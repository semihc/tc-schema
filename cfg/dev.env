# WARNING: this file is tracked by git. Never put real credentials here.
# For passwords or production DSNs, use cfg/dev.env.local (gitignored) or
# set the EOD_DSN environment variable directly.
#EOD_DSN=postgresql://user:pw@host:5433/tcdata
EOD_DSN=postgresql://localhost:5433/tcdata